requests>=2.31
python-escpos>=3.1       # USB transport (pulls pyusb)
pyserial>=3.5            # serial / Bluetooth SPP
pywin32>=306; sys_platform == "win32"   # Windows spooler (win://)
tomli>=2.0; python_version < "3.11"
